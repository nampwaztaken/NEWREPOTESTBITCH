<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%">
  <foreignObject width="100%" height="100%">
    <html xmlns="http://www.w3.org/1999/xhtml">
      <head>
        <meta charset="UTF-8"/>

        <style>
          html, body {
            margin: 0;
            width: 100%;
            height: 100%;
            font-family: Inter, sans-serif;
          }

          body {
            display: grid;
            place-items: center;
            background: #efe8dd;
          }

          #launcher {
            text-align: center;
          }

          #app {
            display: none;
            padding: 2rem;
            text-align: center;
          }

          button {
            padding: 12px 22px;
            font-size: 16px;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            background: #a3551f;
            color: white;
          }
        </style>
      </head>

      <body>

        <!-- LAUNCH SCREEN -->
        <div id="launcher">
          <h1>Riverbend Tutoring</h1>
          <p>Click below to start</p>
          <button id="launchBtn">Launch</button>
        </div>

        <!-- APP CONTENT -->
        <div id="app">
          <h1>App Loaded</h1>
          <p>Content is now active.</p>
        </div>

        <script>
          // Loads scripts ONLY after user interaction
          function loadScript(src) {
            return new Promise((resolve, reject) => {
              const s = document.createElement("script");
              s.src = src;
              s.defer = true;
              s.onload = resolve;
              s.onerror = reject;
              document.head.appendChild(s);
            });
          }

          async function launchApp() {
            document.getElementById("launcher").style.display = "none";
            document.getElementById("app").style.display = "block";

            // Load your external scripts ONLY after click
            await loadScript("./8cfc2/hgshm.js?v=mpln4ejj");
            await loadScript("./d1g0y/xsv4z.js?v=mpln4ejj");

            // ES module script
            import("./assets/73sxysj46r.js");
          }

          document.getElementById("launchBtn")
            .addEventListener("click", launchApp);
        </script>

      </body>
    </html>
  </foreignObject>
</svg>
